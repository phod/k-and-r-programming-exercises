#include <stdio.h>
#define SPACE=0
#define NOSPACE=1

int main() {
  int state,c;
  state = SPACE;
  c = getchar();
  while(c != EOF) {
    if (state) {
      ;
    } else {
      putchar(c);
    }
    c = getchar();
  }
}
