#include <stdio.h>
#define TABSIZE 4

void detab();

int main() {
  int c;

  while ((c = getchar()) != EOF) {
    if (c == '\t') {
      detab();
    } else {
      putchar(c);
    }
  }
  return 0
}

void detab() {
  int i;

  while (i < TABSIZE) {
    printf(" ");
    i++;
  }
}
